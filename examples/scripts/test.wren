class Test {
    static zero() {
        System.print("zero()")
    }

    static one(v) {
        System.print("one(%(v))")
    }

    static two(v1, v2) {
        System.print("two(%(v1), %(v2))")
    }
}